# AI时代的模型引擎

## 背景
大概从去年10月份开始， AI Coding刚开始兴起的时候， 我就第一时间参与了。当时还是写Rule的规则时代，既没有”虾“也没有”马“。在那个时候，我萌生了一个数据模型的想法。数据模型的想法本质还是源自云数据库。它是什么样子呢？或者说他具有哪些能力呢？

1. 它具有安全的Http能力。
2. 它具有完善的RLS能力。
3. 它具有自我驱动的文档能力。
4. 它具有命令行能力。

抱着这4个目标。 我开始了漫长的Vibecoding，直到最近，我终于搞出来了。隆重给大家介绍下我的项目————Modelcraft。AI时代的模型引擎。

目前在 http://lukemxjia.devcloud.woa.com:3100/ devcloud域名。大家可以随意注册使用。 
GitHub地址：https://github.com/patientCat/aritcles-hub


## 什么是模型引擎
模型是数据表的一层抽象。或者说，是对数据库表带有业务维度的解释。
数据库表本身的meta信息不足，比如一个字段是否敏感，一个字段是否可以查询，一个字段代表了怎样的也无逻辑，这些都无法表示。所以需要更高维度的抽象。

### 解决什么问题
首先Mysql数据库表我们大家经常使用。但是假设我是一个前段，或者说，我是一个产品，一个运营，一个设计。我其实是不理解Mysql的。 但是我又有类似访问数据，写数据的需求。 而且很多时候是一个qps非常小，仅仅是自己使用，简单Vibecoding一下就满足我的要求。 但是这里有个关键点。数据一般都在后端那里，而后端往往又是按照优先级进行排，很多时候难以照顾。那么，能否把数据库开放呢？

但这里还有一个遗留问题，就是安全和便利的平衡。一旦采用这套方案，本质上就相当于把数据库的能力直接开放出去了，便利性会非常高，但风险也会同步放大。比如权限怎么隔离、敏感字段怎么控制、误操作怎么兜底、审计怎么补齐，这些都必须提前想清楚。

### 存储在云，计算在端
我自己隐约感受到这样一个概念。 作为一个开发，在深度使用AI的过程中，我平时要用到日志，数据库等诸多中间件，他们对我来说是存储，是在云上的。但是很多时候，我有我自己的定制化需求。 而且这个需求不是像以前那种，计算在云的，需要保证qps，保证安全等等。我这些需求纯为我自己服务。或者为少数几个人服务。这种计算更像是存在本地，通过一个skill描述计算过程。

> 我不知道这是否是未来发展的趋势。但是这种模块化的能力，我知道这里一定是对的。即我们现在只提供的大部分业务接口都是粗的。如果客户能拿到自己的数据，他在这个过程中，利用现有的OpenAPI，是否能像飞书那样，发挥出质变的能力呢？

## 当前 Milestone

回到最初定下的四个目标，截止目前的完成情况：

1. ✅ 安全的 HTTP 能力 —— 已完成
2. 🚧 完善的 RLS 能力 —— 后端逻辑基本就绪，前端设计尚未完成
3. ✅ 自我驱动的文档能力 —— 已完成
4. ✅ 命令行能力 —— 已完成

三个目标已落地，RLS 是唯一剩下的硬骨头。

那么他来了

http://lukemxjia.devcloud.woa.com:3100/

大家可以访问这里，因为是devcloud的域名，而且是内部狗粮的第一版本，我的目的是收集意见。如果大家遇到哪些问题， 可以在评论留言。


### 基本的使用方式（偷懒）

1. 注册组织， 或者不用。 直接用户登录， luke, jmx931228。 这个是可以用来进行测试的账号。 
2. 下载cli端。 利用api Token。 因为我已经内置好了数据库能力， 你可以直接使用。最好的方式是交给ai使用。 
3. 因为这是我自己写的东西。第一次做到了AI全栈，我是有知识的诅咒的，所以我内置了一个AI，不会可以问他，再不会评论里给我留言。
4. 目前我提供了两种使用方式：一种是 CLI，另一种是开放 API。比如在 CodeBuddy 里，你只需要告诉它使用本地的 `mc` 命令，再把你的 `mc_pat_xxxxxx` token 给它，它就可以直接帮你查 `orders` 表。这里的 `mc` 不是 MiniIO，而是我这个模型引擎的命令行入口。

### 使用效果
下面这张图展示的，就是一个最直观的使用场景。

我只需要在 CodeBuddy 里告诉它：

```text
用我本地的 mc 命令，token 是 mc_pat_xxxxxx，帮我查询 orders 表
```

然后 AI 就会自己去做后面的事情：

1. 先找到项目和数据库
2. 再定位到 `orders` 表
3. 读取表结构
4. 生成正确的查询语句
5. 最后把结果整理成表格返回给我

这意味着，用户不需要先理解 MySQL，也不需要自己手写查询语句。只要把需求说清楚，AI 就可以直接调用本地能力，把数据查出来。

![mcclient使用图](./mcclient使用图.png)

除了 CLI，也可以直接用开放 API。只需要在本地放一个 `test.sh`，内容大致如下：

```bash
SERVER_URL="http://lukemxjia.devcloud.woa.com:9080"
TOKEN="mc_pat_xxxxxx"   # 替换为你自己的 token

curl -X POST "${SERVER_URL}/end-user/graphql/org/{org}/project/{project}/db/{db}/model/users" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ${TOKEN}" \
  -d '{"query":"query { findMany(take: 5, skip: 0) { items { id } } }"}'
```

AI 读到这个脚本后，就能自己推断出这是一个 GraphQL 接口，然后按需修改查询目标（比如把 `users` 换成 `orders`），自动完成查询——下图展示的就是这个过程：

![开放文档使用](./开放文档使用.png)

### 完全体

整套系统对用户来说是 **AI 自驱的**。用户通过 GraphQL 自我检索、自动完成使用——GraphQL 的强类型约束天然充当了"护栏"，语法错误可以被 AI 自我感知并纠正，不需要人工介入。

#### 管理员负责什么

管理员做的事，本质上是**划定边界**：

1. **数据库 & 表的接入控制**：决定哪些数据库、哪些表对外暴露，不暴露的对任何人都不可见。
2. **RBAC 权限设计**：为不同角色分配不同的读写权限，确保用户只能操作被授权的资源。
3. **RLS（Row Level Security）**：对标 PostgreSQL 的行级安全策略，做到同一张表里，不同用户只能看到属于自己的数据行（该能力正在开发中）。

管理员不需要关心用户怎么查数据，只需要把"能查什么"的边界划清楚，剩下的交给 AI 和 GraphQL 自动完成。

#### 用户
普通用户只有数据的使用权限。通过 CLI 调用，后期也会支持 OpenAPI 的使用方式。


## 使用场景

### 我最初设想的场景

最开始我想到的场景很朴素：**我在云上有一个自己的数据库，我希望给它加一层 AI 自驱的 HTTP 能力。**

基于这个出发点，延伸出了几个具体的使用方向：

1. **个人 / 小团队的低代码前端**：前端开发者通过 with 等平台快速搭建页面，数据层直接对接 Modelcraft，不需要再写一套后端接口。
2. **测试 & 体验环境的 Harness**：部署一个专门对接测试库或 Demo 库的实例，作为自动化测试流水线的一环，AI 自动生成查询、验证数据，省去手写 fixture 的成本。
3. **云平台售卖数据库能力**：这是我觉得最匹配的场景——云厂商用 Serverless 底层数据库资源，通过 Modelcraft 这一层对个人开发者提供开箱即用的数据库能力，类似 Supabase 的定位，但查询入口是 AI 自驱的 GraphQL，而不是需要自己手写 SQL。

### 一个我至今没想明白的问题

不论哪种场景，有一个绕不开的核心矛盾：

> **Modelcraft 代理了数据库。那么当数据库挂了，算谁的责任？**

如果是用户自己的数据库，Modelcraft 只是一个代理层，数据库宕机理论上与我无关。但用户的感知是"你的服务挂了"。如果是云平台模式，底层数据库资源由平台提供，责任边界更模糊——是数据库层的问题，还是 Modelcraft 层的问题，用户分不清楚，SLA 也很难定义。

这个问题在 Supabase、PlanetScale 这类产品上同样存在，但他们是自己托管数据库，责任主体是清晰的。Modelcraft 做的是**代理**，不拥有数据，这让责任归属变得很微妙。

我目前没有一个满意的答案，欢迎有想法的同学在评论里和我探讨。

## 写在后面的话

我总想着把所有事情都做好、做完善，再发出来。但有时候，这样反而容易闭门造车——自己觉得好的，不一定是别人需要的；自己没想到的问题，旁观者一眼就能看出来。

所以这次我决定先把它分享出来。不完美没关系，RLS 还没做完，使用文档还很粗糙，很多细节还需要打磨。但如果你看完有任何想法——觉得哪个场景有意思、觉得哪里设计有问题、或者只是单纯好奇——都欢迎在评论里告诉我。

后面我也想详细聊聊这套系统的设计过程，从产品到 UI，到前端，到后端，到 Agent，每一层都有值得说的故事。这篇算是个开头。

---

做完这个产品的那一刻，回顾自己五年的云开发之路，能看到很多当初设计的影子——数据模型、权限隔离、接口抽象，这些概念在我脑子里埋了很久。只是那时候的自己还是个菜鸟，很多东西写得欠缺考虑，甚至根本没想清楚就动手了。

这一次不一样。从产品调研、方案设计、架构实现，到云原生与私有化部署，从前端到后端，再到自己独立的 Agent，从流水线到上线——我第一次真正完整地走完了一个产品的全链路。一个人，把这些事情自我 cover 掉了。

或许还有很多不完美。但它是我第一个完整的开源作品，也是我在云开发平台做数据模型多年之后，给自己交的一份答卷。

这个作品，是我写给过去五年的一个交代。

感谢那些曾经帮助过我的人。
