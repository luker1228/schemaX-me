---
import { IndexPage } from "../../../howto/components/frontend/index-page.jsx";
import "../../../styles/global.css";

const title = "后端同学的前端课程";
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <script is:inline>
      (function () {
        try {
          var state = JSON.parse(sessionStorage.getItem("howto-manual-transition") || "null");
          var currentPath = window.location.pathname.replace(/\/+$/, "/");
          if (
            state &&
            state.href &&
            state.expiresAt &&
            state.expiresAt >= Date.now() &&
            state.href.replace(/^https?:\/\/[^/]+/, "").replace(/\/+$/, "/") === currentPath
          ) {
            document.documentElement.classList.add("arrival-pending");
          }
        } catch (e) {}
      })();
    </script>
  </head>
  <body>
    <div class="arrival-mask" id="arrivalMask" aria-hidden="true">
      <div class="arrival-shell">
        <div class="arrival-head">
          <span class="arrival-label">BIO ID SCAN</span>
          <span class="arrival-state">ACCESS RELEASED</span>
        </div>
        <div class="arrival-grid">
          <div class="arrival-frame">
            <i class="arrival-corner tl"></i>
            <i class="arrival-corner tr"></i>
            <i class="arrival-corner bl"></i>
            <i class="arrival-corner br"></i>
            <div class="arrival-noise"></div>
            <div class="arrival-target">
              <div class="arrival-ring ring-a"></div>
              <div class="arrival-ring ring-b"></div>
              <div class="arrival-ring ring-c"></div>
              <div class="arrival-silhouette">
                <i class="arrival-head-shape"></i>
                <i class="arrival-shoulders"></i>
              </div>
              <div class="arrival-beam"></div>
            </div>
            <span class="arrival-percent" id="arrivalPercent">0%</span>
            <span class="arrival-success">LLM 识别成功</span>
          </div>

          <div class="arrival-side">
            <div class="arrival-dossier">
              <span class="arrival-kicker">TARGET DOSSIER</span>
              <strong id="arrivalTitle">前端课程</strong>
              <p id="arrivalPath">&gt; ACCESS // ./howto/frontend</p>
            </div>
            <div class="arrival-log">
              <p>&gt; FACE VECTOR MATCHED</p>
              <p>&gt; CLEARANCE LEVEL // 已解密</p>
              <p>&gt; MANUAL UNSEALED</p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <script is:inline>
      (function () {
        var mask = document.getElementById("arrivalMask");
        if (!mask) return;
        var reduce = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
        var title = document.getElementById("arrivalTitle");
        var path = document.getElementById("arrivalPath");
        var percent = document.getElementById("arrivalPercent");
        var state = null;
        try {
          state = JSON.parse(sessionStorage.getItem("howto-manual-transition") || "null");
        } catch (e) {}
        var currentPath = window.location.pathname.replace(/\/+$/, "/");
        if (
          !state ||
          !state.href ||
          !state.expiresAt ||
          state.expiresAt < Date.now() ||
          state.href.replace(/^https?:\/\/[^/]+/, "").replace(/\/+$/, "/") !== currentPath
        ) {
          mask.remove();
          return;
        }
        try {
          sessionStorage.removeItem("howto-manual-transition");
        } catch (e) {}
        if (title && state.title) title.textContent = state.title;
        if (path && state.path) path.textContent = "> ACCESS // ./" + state.path;
        if (reduce) {
          document.documentElement.classList.remove("arrival-pending");
          mask.remove();
          return;
        }
        document.documentElement.classList.add("arrival-active");
        requestAnimationFrame(function () {
          mask.classList.add("is-active");
          window.setTimeout(function () {
            mask.classList.add("is-armed");
            window.setTimeout(function () {
              mask.classList.add("is-opening");
              window.setTimeout(function () {
                var current = 0;
                var timer = window.setInterval(function () {
                  current += 4;
                  if (current >= 100) {
                    current = 100;
                    window.clearInterval(timer);
                    mask.classList.add("is-verified");
                  }
                  if (percent) percent.textContent = current + "%";
                }, 58);
              }, 120);
              window.setTimeout(function () {
                mask.classList.add("is-closing");
                window.setTimeout(function () {
                  mask.classList.add("is-done");
                  document.documentElement.classList.remove("arrival-pending");
                  document.documentElement.classList.remove("arrival-active");
                  window.setTimeout(function () {
                    mask.remove();
                  }, 240);
                }, 760);
              }, 1700);
            }, 260);
          }, 40);
        });
      })();
    </script>

    <div data-arrival-content>
      <IndexPage client:load />
    </div>
  </body>
</html>

<style>
  :global(html.arrival-pending) {
    background: var(--charcoal);
  }
  :global(html.arrival-active),
  :global(html.arrival-active body) {
    overflow: hidden;
  }
  :global(html.arrival-pending body) {
    background: var(--charcoal);
  }
  :global(html.arrival-pending [data-arrival-content]) {
    opacity: 0;
  }
  :global(html.arrival-active .brand-name) {
    opacity: 0;
  }
  [data-arrival-content] {
    transition: opacity 320ms ease 180ms;
  }
  .arrival-mask {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: grid;
    place-items: center;
    padding: 2rem;
    background: var(--dossier-boot-bg);
    color: var(--phosphor);
    font-family: "IBM Plex Mono", "JetBrains Mono", ui-monospace, monospace;
    opacity: 0;
    visibility: hidden;
    overflow: hidden;
    transition: opacity 220ms ease, visibility 220ms ease;
  }
  .arrival-mask::before {
    content: "";
    position: absolute;
    left: 0;
    top: 50%;
    z-index: 4;
    width: 100%;
    height: 1px;
    background: color-mix(in srgb, var(--paper-code) 34%, transparent);
    box-shadow: 0 0 18px color-mix(in srgb, var(--phosphor-green) 32%, transparent);
    opacity: 1;
    transform: translateY(-50%);
    transition: opacity 420ms ease;
  }
  .arrival-mask.is-opening::before {
    opacity: 0;
  }
  .arrival-mask.is-active {
    opacity: 1;
    visibility: visible;
  }
  :global(html.arrival-pending) .arrival-mask {
    opacity: 1;
    visibility: visible;
  }
  .arrival-mask.is-done {
    opacity: 0;
    visibility: hidden;
    transition: opacity 220ms ease, visibility 220ms ease;
  }
  .arrival-shell {
    width: min(760px, 100%);
    position: relative;
    z-index: 2;
    padding: clamp(1.1rem, 3vw, 1.6rem);
    border: 1px solid color-mix(in srgb, var(--mint) 28%, transparent);
    border-radius: 24px;
    background:
      linear-gradient(180deg, color-mix(in srgb, var(--dark-void) 72%, transparent), color-mix(in srgb, var(--text) 90%, transparent)),
      color-mix(in srgb, var(--text) 94%, transparent);
    box-shadow:
      0 0 0 1px color-mix(in srgb, var(--paper-card) 4%, transparent) inset,
      0 24px 80px color-mix(in srgb, var(--text) 42%, transparent),
      0 0 48px color-mix(in srgb, var(--phosphor-green) 12%, transparent);
    overflow: hidden;
    opacity: 0;
    transform: translateY(10px) scale(0.985);
    transition: opacity 360ms ease 80ms, transform 520ms cubic-bezier(0.2, 0.78, 0.18, 1) 80ms;
  }
  .arrival-mask.is-armed .arrival-shell {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
  .arrival-mask.is-closing .arrival-shell {
    opacity: 0;
    transform: translateY(-8px) scale(0.985);
    transition-delay: 0ms;
  }
  .arrival-shell::before,
  .arrival-shell::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
  }
  .arrival-shell::before {
    background:
      radial-gradient(circle at top right, color-mix(in srgb, var(--phosphor-green) 18%, transparent), transparent 28%),
      linear-gradient(90deg, transparent, color-mix(in srgb, var(--paper-card) 5%, transparent), transparent);
    opacity: 0.8;
  }
  .arrival-shell::after {
    background: repeating-linear-gradient(0deg, color-mix(in srgb, var(--paper-card) 3%, transparent) 0 1px, transparent 1px 4px);
    mix-blend-mode: screen;
    opacity: 0.3;
  }
  .arrival-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1rem;
    font-size: 0.74rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }
  .arrival-label {
    color: color-mix(in srgb, var(--paper-card) 74%, transparent);
  }
  .arrival-state {
    color: var(--yellow);
  }
  .arrival-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(260px, 0.95fr);
    gap: 1.15rem;
    align-items: stretch;
  }
  .arrival-frame,
  .arrival-side {
    position: relative;
    border: 1px solid color-mix(in srgb, var(--mint) 22%, transparent);
    border-radius: 20px;
    background: color-mix(in srgb, var(--text) 70%, transparent);
  }
  .arrival-frame {
    min-height: 360px;
    padding: 1rem;
    overflow: hidden;
  }
  .arrival-corner {
    position: absolute;
    width: 22px;
    height: 22px;
    border-color: var(--paper-code);
    border-style: solid;
    border-width: 0;
    opacity: 0.88;
  }
  .arrival-corner.tl {
    top: 12px;
    left: 12px;
    border-top-width: 2px;
    border-left-width: 2px;
  }
  .arrival-corner.tr {
    top: 12px;
    right: 12px;
    border-top-width: 2px;
    border-right-width: 2px;
  }
  .arrival-corner.bl {
    bottom: 12px;
    left: 12px;
    border-bottom-width: 2px;
    border-left-width: 2px;
  }
  .arrival-corner.br {
    right: 12px;
    bottom: 12px;
    border-right-width: 2px;
    border-bottom-width: 2px;
  }
  .arrival-noise {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(180deg, color-mix(in srgb, var(--phosphor-green) 5%, transparent), transparent 24%, transparent 76%, color-mix(in srgb, var(--phosphor-green) 6%, transparent)),
      repeating-linear-gradient(90deg, color-mix(in srgb, var(--phosphor-green) 6%, transparent) 0 1px, transparent 1px 48px),
      repeating-linear-gradient(0deg, color-mix(in srgb, var(--phosphor-green) 5%, transparent) 0 1px, transparent 1px 40px);
    opacity: 0.65;
  }
  .arrival-target {
    position: relative;
    min-height: 328px;
    display: grid;
    place-items: center;
  }
  .arrival-ring {
    position: absolute;
    border: 1px solid color-mix(in srgb, var(--paper-code) 34%, transparent);
    border-radius: 50%;
  }
  .arrival-ring.ring-a {
    width: 210px;
    height: 210px;
  }
  .arrival-ring.ring-b {
    width: 154px;
    height: 154px;
  }
  .arrival-ring.ring-c {
    width: 282px;
    height: 282px;
    border-style: dashed;
    opacity: 0.6;
    animation: arrival-ring-spin 3200ms linear infinite;
  }
  .arrival-beam {
    position: absolute;
    left: 10%;
    right: 10%;
    top: 0;
    height: 72px;
    background: linear-gradient(180deg, transparent, color-mix(in srgb, var(--paper-code) 42%, transparent), transparent);
    filter: blur(2px);
    transform: translateY(-120%);
    animation: arrival-beam-scan 1800ms ease-in-out 120ms forwards;
  }
  .arrival-silhouette {
    position: relative;
    width: 168px;
    height: 208px;
  }
  .arrival-head-shape,
  .arrival-shoulders {
    position: absolute;
    left: 50%;
    display: block;
    transform: translateX(-50%);
    background: linear-gradient(180deg, color-mix(in srgb, var(--paper-code) 92%, transparent), color-mix(in srgb, var(--phosphor-green) 16%, transparent));
    box-shadow: 0 0 18px color-mix(in srgb, var(--mint) 16%, transparent);
  }
  .arrival-head-shape {
    top: 8px;
    width: 70px;
    height: 82px;
    border-radius: 40px 40px 34px 34px;
  }
  .arrival-shoulders {
    bottom: 12px;
    width: 164px;
    height: 124px;
    border-radius: 72px 72px 28px 28px;
    clip-path: polygon(7% 100%, 19% 38%, 34% 18%, 50% 10%, 66% 18%, 81% 38%, 93% 100%);
  }
  .arrival-percent {
    position: absolute;
    right: 18px;
    bottom: 18px;
    color: var(--paper-code);
    font-size: 0.84rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    animation: arrival-percent-flicker 900ms steps(2, end) 240ms infinite;
  }
  .arrival-success {
    position: absolute;
    left: 18px;
    bottom: 18px;
    padding: 0.36rem 0.65rem;
    border: 1px solid color-mix(in srgb, var(--paper-code) 42%, transparent);
    background: color-mix(in srgb, var(--text) 72%, transparent);
    color: var(--paper-card);
    font-size: clamp(1rem, 2.4vw, 1.22rem);
    font-weight: 700;
    letter-spacing: 0.1em;
    opacity: 0;
    transform: translateY(4px);
    transition: opacity 180ms ease, transform 180ms ease;
  }
  .arrival-mask.is-verified .arrival-success {
    opacity: 1;
    transform: translateY(0);
  }
  .arrival-side {
    display: grid;
    gap: 1rem;
    padding: 1rem;
    align-content: center;
  }
  .arrival-kicker {
    color: color-mix(in srgb, var(--paper-card) 58%, transparent);
    font-size: 0.68rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }
  .arrival-dossier {
    display: grid;
    gap: 0.45rem;
  }
  .arrival-dossier strong {
    color: var(--paper-card);
    font-size: clamp(1.15rem, 3vw, 1.5rem);
    line-height: 1.05;
    letter-spacing: 0.02em;
  }
  .arrival-dossier p {
    margin: 0;
    color: color-mix(in srgb, var(--paper-code) 90%, transparent);
    font-size: 0.82rem;
    line-height: 1.6;
  }
  .arrival-log {
    width: 100%;
    color: var(--phosphor-green);
    font-size: 0.78rem;
    line-height: 1.7;
    padding: 0.95rem 1rem;
    border: 1px solid color-mix(in srgb, var(--mint) 15%, transparent);
    border-radius: 14px;
    background: color-mix(in srgb, var(--charcoal) 68%, transparent);
  }
  .arrival-log p {
    margin: 0;
    opacity: 0;
    animation: arrival-line 220ms ease forwards;
  }
  .arrival-log p:nth-child(2) {
    animation-delay: 120ms;
  }
  .arrival-log p:nth-child(3) {
    animation-delay: 240ms;
  }
  @keyframes arrival-line {
    to {
      opacity: 1;
    }
  }
  @keyframes arrival-ring-spin {
    to {
      transform: rotate(1turn);
    }
  }
  @keyframes arrival-beam-scan {
    to {
      transform: translateY(360px);
    }
  }
  @keyframes arrival-percent-flicker {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.56;
    }
  }
  @media (max-width: 820px) {
    .arrival-grid {
      grid-template-columns: 1fr;
    }
    .arrival-frame {
      min-height: 300px;
    }
    .arrival-target {
      min-height: 268px;
    }
    .arrival-ring.ring-c {
      width: 236px;
      height: 236px;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .arrival-mask,
    .arrival-ring.ring-c,
    .arrival-beam,
    .arrival-log p {
      animation: none;
      transition: none;
    }
  }
</style>
